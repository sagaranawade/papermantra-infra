# Optional local overrides only (production uses committed .env on main)
.env.local
.env.*.local

# Certbot live data (generated on the VPS)
certbot/conf/
certbot/www/

# Deployment state on server
.deploy-history
.deploy-lock
backups/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
