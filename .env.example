# =============================================================================
# DEPRECATED — production config is in /.env (committed on main).
# Edit .env in the repo and push; VPS uses git pull.
# =============================================================================
# See: .env
